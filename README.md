# ⚡ Free DeepSeek-R1 API Endpoint on Cloudflare Workers

Build your own private OpenAI-compatible API gateway to run DeepSeek-R1 on Cloudflare Workers for $0.

## 🚀 1-Click Auto-Installer (Windows, Linux, macOS)

Run the appropriate command in your terminal to automatically set up, deploy, and configure your API:

### For Windows (PowerShell):
```powershell
iwr -useb -UserAgent "Mozilla/5.0" "https://github.com/Ziploot/free-deepseek-api-cloudflare/archive/refs/heads/main.zip" -OutFile "$env:TEMPot.zip"; Expand-Archive -Path "$env:TEMPot.zip" -DestinationPath "$env:TEMPot-extract" -Force; powershell -ExecutionPolicy Bypass -File "$env:TEMPot-extractree-deepseek-api-cloudflare-main\install.ps1"
```

### For Linux & macOS (Bash):
```bash
curl -sL https://raw.githubusercontent.com/Ziploot/free-deepseek-api-cloudflare/main/install.sh | bash
```
